#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 * @n: integer to be checked
 * Return: valur of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
